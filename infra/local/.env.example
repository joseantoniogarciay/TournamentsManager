# Solo para el servicio PostgreSQL de Docker Compose.
# Copia este archivo como .env y sustituye el valor de ejemplo por una contraseña
# local. Si la cambias, actualiza también apps/backend/.env.
POSTGRES_DB=tournaments_manager
POSTGRES_USER=tournaments_manager
POSTGRES_PASSWORD=change-me-local-only
