global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: tournaments-manager-api
    metrics_path: /metrics
    static_configs:
      - targets: [api:8080]
