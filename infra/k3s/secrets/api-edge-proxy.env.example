# Credencial exclusiva del borde Caddy hacia la API de prod.
# Copiar fuera de Git como api-edge-proxy.env; debe ser aleatoria y no contener
# espacios ni saltos de línea.
EDGE_PROXY_AUTH_TOKEN=replace-with-a-64-character-random-hex-token
