root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/api ./cmd/api"
  entrypoint = ["./tmp/api"]
  delay = 500
  exclude_dir = ["tmp", "vendor"]
  include_ext = ["go"]
  stop_on_error = true

[log]
  time = true
